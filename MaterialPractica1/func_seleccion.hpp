#ifndef FUNC_SELECCION_HPP
#define FUNC_SELECCION_HPP

#include <vector>

using namespace std;

    void rellenarVector(vector<int> &v);
    void ordenacionSeleccion(vector<int>&v);
    bool estaOrdenado(const vector<int> &v);

    void imprimirVector(const vector<int> &v);

#endif